import s from "./Window.module.css";

const Window = (props) => {
    return (
        <div className={s.wrapper}>
            {/*{props.order === 1 ? (*/}
            {/*    <div className={`${s.wrapper} ${s.o1}`}>*/}
            {/*        <div className={`${s.window}`}></div>*/}
            {/*    </div>*/}
            {/*) : (*/}
            {/*    <div className={`${s.wrapper} ${s.o2}`}>*/}
            {/*        <div className={`${s.window}`}></div>*/}
            {/*    </div>*/}
            {/*)}*/}
        </div>
    );
};

export default Window;
